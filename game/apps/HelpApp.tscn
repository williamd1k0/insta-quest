[gd_scene load_steps=2 format=2]

[ext_resource path="res://game/apps/TextApp.tscn" type="PackedScene" id=1]

[node name="HelpApp" instance=ExtResource( 1 )]

[node name="Bg" parent="." index="0"]
color = Color( 0.713726, 0.266667, 0.862745, 1 )

[node name="Title" parent="." index="1"]
custom_colors/font_color = Color( 0.490196, 0.0941176, 0.619608, 1 )
text = "Help"

[node name="ScrollText" parent="." index="2"]
bbcode_text = "(AGBIC build)

Run the Insta-E app to start the game.

Use [img]res://assets/textures/minigames/notify-take.png[/img] R1/A (Space) to take pictures at the right time.
Use [img]res://assets/textures/minigames/notify-dpad.png[/img] d-pad (arrow keys) to move when needed.

Be careful, you can take bad pictures  for only 3 times!
"
text = "(AGBIC build)

Run the Insta-E app to start the game.

Use  R1/A (Space) to take pictures at the right time.
Use  d-pad (arrow keys) to move when needed.

Be careful, you can take bad pictures  for only 3 times!
"

